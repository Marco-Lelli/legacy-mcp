## Commit Workflow

1. Run `python -m pytest` and confirm all tests pass
2. Run `git status` and display all changed/staged files
3. Ask user to confirm which files to stage
4. Stage confirmed files and show the diff summary
5. Ask user for commit message, then commit
